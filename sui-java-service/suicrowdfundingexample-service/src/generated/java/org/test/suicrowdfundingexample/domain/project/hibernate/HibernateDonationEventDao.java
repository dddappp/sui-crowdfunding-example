// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.suicrowdfundingexample.domain.project.hibernate;

import java.math.BigInteger;
import java.util.Date;
import org.test.suicrowdfundingexample.domain.*;
import org.hibernate.*;
import org.hibernate.criterion.*;
import org.test.suicrowdfundingexample.domain.project.*;
import org.test.suicrowdfundingexample.specialization.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateDonationEventDao implements DonationEventDao {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }

    @Override
    public void save(DonationEvent e)
    {
        getCurrentSession().save(e);
        if (e instanceof Saveable)
        {
            Saveable saveable = (Saveable) e;
            saveable.save();
        }
    }


    @Transactional(readOnly = true)
    @Override
    public Iterable<DonationEvent> findByProjectEventId(ProjectEventId projectEventId)
    {
        Criteria criteria = getCurrentSession().createCriteria(AbstractDonationEvent.class);
        Junction partIdCondition = Restrictions.conjunction()
            .add(Restrictions.eq("donationEventId.projectId", projectEventId.getId()))
            .add(Restrictions.eq("donationEventId.version", projectEventId.getVersion()))
            ;
        return criteria.add(partIdCondition).list();
    }

}

